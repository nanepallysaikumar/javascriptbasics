
// es6 new feature of strings -- "string literals"
let name = "saikumar";
const dateOfbirth = 1990;

function getAge(age) {
    return 2019 - age;
}

console.log(`${name} age is ${getAge(dateOfbirth)}`);
